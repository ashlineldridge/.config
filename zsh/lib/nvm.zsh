# This file configures NVM according to the instructions here:
# https://github.com/nvm-sh/nvm#installing-and-updating. NVM is installed by
# curling and executing the installation script as specified in the
# documentation. NVM_DIR is exported by lib/env.zsh and overrides the default
# installation directory.

source "${NVM_DIR}/nvm.sh"
